/**
 * Reactive Controllers for Package Browser
 *
 * Controllers manage side effects (timers, abort signals, caching) separately
 * from component rendering logic, following Lit's composition patterns.
 */

export { SearchController } from './searchController';
export { DetailsController } from './detailsController';
