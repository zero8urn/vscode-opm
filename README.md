# DPM: Dotnet Package Manager VS Code Extension



Prerequisites:
- Node.js LTS (24+)
- VS Code with Extension Development Host

Quick steps:

```bash
cd ./vscode-dpm
npm install
npm run build
# open the project folder in VS Code and press F5
```